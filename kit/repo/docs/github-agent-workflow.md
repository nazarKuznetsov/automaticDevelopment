# GitHub Agent Workflow v2

This compatibility entry intentionally contains no second copy of the workflow. The canonical, publishable guide is split across `docs/guide/*.md`, beginning with [Quickstart](guide/quickstart.md).

Machine configuration lives in `.codex/agent-workflow.json`; versioned packet schemas live in `.codex/schemas/v2/`; the approved product contract lives in `docs/product/canonical.md`.
