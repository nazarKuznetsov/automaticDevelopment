interface:
  display_name: "GitHub Project Planner"
  short_description: "Build and repair schema-valid Issue plans"
  default_prompt: "Use $github-project-planner in a fresh read-only task to produce or repair complete digest-bound Global Roadmap and Phase Plan JSON packets, validate them, and make no GitHub writes."
