interface:
  display_name: "GitHub Agent Orchestrator"
  short_description: "Materialize or execute an approved plan safely"
  default_prompt: "Use $github-agent-orchestrator in a fresh top-level task to validate and exactly materialize the approved digest-bound contracts in the authorized Start Packet mode; create Workers only in wave_execution."
