interface:
  display_name: "GitHub Agent Orchestrator"
  short_description: "Execute approved GitHub rolling waves safely"
  default_prompt: "Use $github-agent-orchestrator to execute the approved Ready wave with strict admission gates."
